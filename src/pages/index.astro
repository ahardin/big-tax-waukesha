---
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card.astro";
import supervisorsData from "../data/supervisors.json";

export interface Supervisor {
	name: string;
	email: string;
	address: string;
	phone: string;
	district: string;
	committeeAssignment: string;
	profilePictureUrl: string;
	detailedProfileUrl: string;
}

const typedSupervisorsData: Supervisor[] = supervisorsData;
---

<Layout title="Welcome to Astro.">
	<main class="container mx-auto max-w-[1200px] px-4 my-12">
		<div class="gap-8 flex flex-col mt-8">
			<h1 class="">Big Tax Waukesha County</h1>

			<p class="instructions text-lg text-center">
				On October 22nd, 2024, the Waukesha County Board of Supervisors
				will hold a vote to create a permament sales tax for the county.
			</p>

			<p class="text-center text-lg">
				This site will record which of the 25 supervisors are <strong
					>big taxers</strong
				> and which retain conservative values.
			</p>

			<p class="text-center text-lg">
				<a
					class="bg-purple-600 text-white px-4 py-2 rounded-md hover:bg-purple-700 
					font-bold transition-colors"
					href="https://waukeshacounty.maps.arcgis.com/apps/webappviewer/index.html?id=bf66a80186324f59870f1d68777cbfbb"
					target="_blank">Find your Supervisor</a
				>
			</p>

			<p class="text-center text-lg">
				Is your supervisor a big taxer? Check back the night of October
				22nd, 2024 to find out!
			</p>

			<div class="flex flex-wrap gap-4 justify-center">
				{
					typedSupervisorsData.map((supervisor) => (
						<Card {supervisor} />
					))
				}
			</div>
		</div>
	</main>
</Layout>

<style>
	main {
		color: white;
	}
	.astro-a {
		/* position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1; */
	}
	h1 {
		font-size: 4rem;
		font-weight: 700;
		text-align: center;
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(
			rgba(var(--accent-dark), 66%),
			rgba(var(--accent-dark), 33%)
		);
		padding: 1.5rem;
		border-radius: 8px;
	}

	.instructions strong {
		color: rgb(var(--accent-light));
	}
</style>
